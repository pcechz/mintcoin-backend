apiVersion: v1
kind: Service
metadata:
  name: ledger-service
  namespace: core
spec:
  selector:
    app: ledger-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
