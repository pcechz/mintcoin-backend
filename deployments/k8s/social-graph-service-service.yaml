apiVersion: v1
kind: Service
metadata:
  name: social-graph-service
  namespace: core
spec:
  selector:
    app: social-graph-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
