apiVersion: v1
kind: Service
metadata:
  name: room-service
  namespace: realtime
spec:
  selector:
    app: room-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
