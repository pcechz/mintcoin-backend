apiVersion: v1
kind: Service
metadata:
  name: referral-service
  namespace: core
spec:
  selector:
    app: referral-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
