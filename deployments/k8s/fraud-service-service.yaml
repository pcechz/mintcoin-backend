apiVersion: v1
kind: Service
metadata:
  name: fraud-service
  namespace: admin
spec:
  selector:
    app: fraud-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
