apiVersion: v1
kind: Service
metadata:
  name: wallet-service
  namespace: core
spec:
  selector:
    app: wallet-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
