apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: core
spec:
  selector:
    app: payment-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
