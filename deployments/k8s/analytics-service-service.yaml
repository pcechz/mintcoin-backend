apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: core
spec:
  selector:
    app: analytics-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
