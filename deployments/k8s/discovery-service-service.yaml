apiVersion: v1
kind: Service
metadata:
  name: discovery-service
  namespace: core
spec:
  selector:
    app: discovery-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
