apiVersion: v1
kind: Service
metadata:
  name: kyc-service
  namespace: core
spec:
  selector:
    app: kyc-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
