apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: core
spec:
  selector:
    app: api-gateway
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
