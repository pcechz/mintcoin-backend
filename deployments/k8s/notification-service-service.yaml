apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: realtime
spec:
  selector:
    app: notification-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
